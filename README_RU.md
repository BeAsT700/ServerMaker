# 🎮 ServerMaker

<div align="center">

![Python](https://img.shields.io/badge/Python-3.8+-3776AB?style=for-the-badge&logo=python&logoColor=white)
![Flask](https://img.shields.io/badge/Flask-2.0+-000000?style=for-the-badge&logo=flask&logoColor=white)
![Bootstrap](https://img.shields.io/badge/Bootstrap-5.3-7952B3?style=for-the-badge&logo=bootstrap&logoColor=white)
![License](https://img.shields.io/badge/License-CC%20BY--NC%204.0-blue?style=for-the-badge)

**Современная веб-панель для управления серверами Minecraft**

[Возможности](#-возможности) • [Установка](#-установка) • [Запуск](#-запуск) • [Использование](#-использование) • [API](#-api)

</div>

---

## 📋 Описание

**ServerMaker** — это легковесная и удобная веб-панель для создания и управления серверами Minecraft. Написана на Python с использованием Flask и предоставляет современный интерфейс с поддержкой темной темы.

### Ключевые особенности:
- 🚀 **Быстрый старт** — создание сервера в несколько кликов
- 🎨 **Современный UI** — красивый интерфейс с поддержкой светлой/темной темы
- 📊 **Мониторинг в реальном времени** — консоль, статус, игроки онлайн
- 👥 **Управление игроками** — OP, бан, кик, whitelist, телепорт, эффекты
- 📁 **Файловый менеджер** — просмотр, редактирование, загрузка файлов
- 💾 **Система бэкапов** — создание, восстановление, загрузка резервных копий

---

## ✨ Возможности

### 🖥️ Управление серверами
| Функция | Описание |
|---------|----------|
| Создание | Автоматическая загрузка ядра (Vanilla, Fabric, Forge) |
| Запуск/Стоп | Управление состоянием сервера |
| Консоль | Веб-консоль с историей команд и автопрокруткой |
| Настройки | Редактирование `server.properties` через GUI |

### 👤 Управление игроками
| Функция | Описание |
|---------|----------|
| Просмотр | Список всех игроков (онлайн/оффлайн) с аватарами |
| Статистика | Здоровье, голод, XP, координаты, эффекты |
| Администрирование | OP/De-OP, Kick, Ban/Unban, Whitelist |
| Действия | Телепорт, эффекты, heal, feed, gamemode, XP |

### 📂 Файловый менеджер
- Навигация по директориям сервера
- Просмотр и редактирование текстовых файлов
- Загрузка файлов (включая drag & drop папок)
- Удаление файлов и директорий

### 💾 Система бэкапов
- Создание резервных копий (ZIP)
- Восстановление из бэкапа
- Загрузка/скачивание бэкапов
- Удаление старых бэкапов

---

## 📦 Требования

### Системные требования

| Компонент | Требование |
|-----------|------------|
| **ОС** | Windows 10/11, Linux, macOS |
| **Python** | 3.8 или новее |
| **Java** | JDK 17+ (для Minecraft 1.18+) |
| **RAM** | Минимум 2 ГБ свободной памяти |
| **Порты** | 1010 (панель), 25565+ (серверы) |

### Python зависимости

```
flask>=2.0
requests>=2.25
nbtlib>=2.0
```

---

## 🚀 Установка

### Windows

```powershell
# 1. Клонируйте репозиторий (или скачайте ZIP)
git clone https://github.com/skerkus/ServerMaker.git
cd ServerMaker

# 2. Создайте виртуальное окружение
python -m venv .venv

# 3. Активируйте окружение
.\.venv\Scripts\Activate.ps1

# 4. Установите зависимости
pip install flask requests nbtlib
```

### Linux / macOS

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/skerkus/ServerMaker.git
cd ServerMaker

# 2. Создайте виртуальное окружение
python3 -m venv .venv

# 3. Активируйте окружение
source .venv/bin/activate

# 4. Установите зависимости
pip install flask requests nbtlib
```

### Проверка Java

Убедитесь, что Java установлена и доступна:

```bash
java -version
```

Если Java не установлена:
- **Windows**: Скачайте [Adoptium JDK](https://adoptium.net/) или [Oracle JDK](https://www.oracle.com/java/technologies/downloads/)
- **Linux**: `sudo apt install openjdk-17-jdk` (Ubuntu/Debian)
- **macOS**: `brew install openjdk@17`

---

## ▶️ Запуск

### Стандартный запуск

```bash
# Активируйте виртуальное окружение (если не активировано)
# Windows:
.\.venv\Scripts\Activate.ps1
# Linux/macOS:
source .venv/bin/activate

# Запустите приложение
python main.py
```

### Вывод при успешном запуске

```
Checking system requirements...
Starting ServerMaker on http://localhost:1010
```

### Доступ к панели

Откройте браузер и перейдите по адресу:

🌐 **http://localhost:1010**

Для доступа из локальной сети используйте IP вашего компьютера:

🌐 **http://YOUR_IP:1010**

---

## 📖 Использование

### Создание сервера

1. Нажмите **"Create Server"** на главной странице
2. Заполните форму:
   - **Название** — имя вашего сервера
   - **Ядро** — Vanilla, Fabric или Forge
   - **Версия** — версия Minecraft (например, 1.20.4)
   - **RAM** — выделяемая память (в ГБ)
   - **EULA** — согласие с лицензией Mojang
3. Нажмите **"Create"**

### Управление сервером

На странице сервера доступны вкладки:

| Вкладка | Описание |
|---------|----------|
| **Console** | Консоль сервера с вводом команд |
| **Files** | Файловый менеджер |
| **Players** | Список и управление игроками |
| **Properties** | Настройки server.properties |
| **Backups** | Управление резервными копиями |

### Работа с консолью

- Вводите команды без `/` (например: `say Hello`, `op Player`)
- Используйте **↑/↓** для навигации по истории команд
- Кнопка паузы останавливает автопрокрутку

### Управление игроками

1. Перейдите на вкладку **Players**
2. Кликните на карточку игрока для подробной информации
3. Доступные действия:
   - **KICK** — кикнуть игрока
   - **BAN/UNBAN** — заблокировать/разблокировать
   - **WHITELIST** — добавить/удалить из whitelist
   - **OP/DE-OP** — выдать/забрать права оператора
   - **TELEPORT** — телепортировать к координатам/игроку
   - **EFFECT** — наложить эффект
   - **HEAL/FEED** — исцелить/накормить
   - **GAMEMODE** — изменить режим игры
   - **+XP** — выдать опыт

---

## 🔌 API

ServerMaker предоставляет REST API для интеграции:

### Основные эндпоинты

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/status/<id>` | Статус сервера |
| POST | `/api/start/<id>` | Запустить сервер |
| POST | `/api/stop/<id>` | Остановить сервер |
| POST | `/api/restart/<id>` | Перезапустить сервер |
| GET/POST | `/api/console/<id>` | Логи / отправка команды |
| GET | `/api/players/<id>` | Список игроков |
| GET | `/api/player_details/<id>/<uuid>` | Данные игрока (NBT) |
| GET | `/api/files/<id>?path=` | Список файлов |
| GET/POST | `/api/properties/<id>` | Настройки сервера |
| GET | `/api/backups/<id>` | Список бэкапов |
| POST | `/api/backup/<id>` | Создать бэкап |

### Пример использования

```python
import requests

# Запустить сервер
response = requests.post('http://localhost:1010/api/start/1234567890')
print(response.json())

# Отправить команду
response = requests.post(
    'http://localhost:1010/api/console/1234567890',
    json={'command': 'say Hello from API!'}
)
```

---

## 📁 Структура проекта

```
ServerMaker/
├── main.py              # Точка входа
├── app.py               # Flask приложение и роуты
├── server_manager.py    # Логика управления серверами
├── README.md            # Документация
│
├── static/              # Frontend ресурсы
│   ├── script.js        # JavaScript логика
│   ├── style.css        # Основные стили
│   ├── style_modals.css # Стили модальных окон
│   ├── style_player.css # Стили страницы игроков
│   └── style_3d.css     # 3D эффекты для предметов
│
├── templates/           # HTML шаблоны (Jinja2)
│   ├── layout.html      # Базовый шаблон
│   ├── index.html       # Главная страница
│   ├── create.html      # Создание сервера
│   └── server.html      # Панель управления сервером
│
├── data/                # Данные панели
│   └── servers.json     # Конфигурация серверов
│
├── servers/             # Директории серверов
│   └── <server_id>/     # Файлы конкретного сервера
│
└── backups/             # Резервные копии
    └── <server_id>/     # Бэкапы конкретного сервера
```

---

## ⚙️ Конфигурация

### Изменение порта панели

В файле `main.py`:

```python
app.run(host='0.0.0.0', port=1010, debug=False)
#                       ^^^^ измените порт здесь
```

### Ограничения памяти

При создании сервера RAM указывается в ГБ. Рекомендации:
- **Vanilla (1-5 игроков)**: 2-4 ГБ
- **Vanilla (5-20 игроков)**: 4-8 ГБ
- **Modded (Forge/Fabric)**: 6-12 ГБ

---

## 🔧 Решение проблем

### Java не найдена

```
CRITICAL ERROR: Java is not installed or not found in PATH.
```

**Решение**: Установите Java и добавьте её в PATH, или перезапустите терминал после установки.

### Порт занят

```
Address already in use
```

**Решение**: Измените порт в `main.py` или завершите процесс, занимающий порт.

### Сервер не запускается

1. Проверьте наличие `server.jar` в папке сервера
2. Проверьте `eula.txt` — должно быть `eula=true`
3. Проверьте логи в консоли панели

### Файлы игроков не загружаются

Убедитесь, что:
1. Игрок хотя бы раз заходил на сервер
2. Мир сохранён (`save-all` в консоли)
3. Папка `world/playerdata/` существует

---

## 📄 Лицензия

Проект распространяется под лицензией **Creative Commons Attribution-NonCommercial 4.0 International (CC BY-NC 4.0)**.

### Вы можете:
- ✅ **Использовать** — запускать и использовать в личных/образовательных целях
- ✅ **Изменять** — адаптировать и дорабатывать проект
- ✅ **Распространять** — копировать и распространять в любом формате

### При соблюдении условий:
- 📛 **Некоммерческое использование** — вы НЕ можете использовать проект в коммерческих целях без явного разрешения автора
- 📝 **Указание авторства** — необходимо указывать автора проекта

Для вопросов о коммерческом использовании свяжитесь с автором.

---

## 🤝 Вклад в проект

Приветствуются pull requests и issues!

1. Fork репозитория
2. Создайте feature branch (`git checkout -b feature/amazing`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push в branch (`git push origin feature/amazing`)
5. Откройте Pull Request

---

<div align="center">

**Сделано с ❤️ для Minecraft сообщества**

</div>
